@{
  Layout = "_Layout";
}

@model AnimalShelter.Models.Animal //Type, Name, Date of Admittance, Breed

<h4>Add a new animal</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Type, new {@class="hello"})
  @Html.TextBoxFor(model => model.Type)
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  @Html.LabelFor(model => model.DateOfAdmittance)
  @Html.TextBoxFor(model => model.DateOfAdmittance, new {type="date"})
  @Html.LabelFor(model => model.Breed)
  @Html.TextBoxFor(model => model.Breed)
    <input type="submit" value="Add new animal" />
}
<p>@Html.ActionLink("Show all animals", "Index")</p>